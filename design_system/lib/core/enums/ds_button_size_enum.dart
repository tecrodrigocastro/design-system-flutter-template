/// Size of a `DsButton`, controlling its height, padding, and font size.
enum DsButtonSize {
  /// Compact button for dense layouts (e.g. inline with a table row).
  small,

  /// Default button size, suitable for most forms and toolbars.
  medium,

  /// Prominent button for hero sections or primary calls to action.
  large,
}

/// Size of a `DsBanner`, controlling its padding, image, and text scale.
enum DsBannerSize {
  /// Compact banner for dense layouts, e.g. inline in a list.
  small,

  /// Default banner size, suitable for most screens.
  medium,

  /// Prominent banner for hero/promotional placements.
  large,
}
